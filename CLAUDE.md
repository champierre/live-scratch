# CLAUDE.md

## Scratch Program Editing

When given instructions about Scratch programs, directly edit the `project.json` and asset files (SVG, WAV, PNG, etc.) under the `workspace/` folder. Do not use the Scratch GUI or any external tools — modify the files in place.
